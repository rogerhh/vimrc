syntax on

syntax enable

set hlsearch

set number

set backspace=indent,eol,start

if has("vms")
    set nobackup    " do not keep a backup file
else
    set backup      " keep a backup file
endif

"set updodir=~/vim
set cursorline
